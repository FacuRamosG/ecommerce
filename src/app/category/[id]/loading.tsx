import React from 'react';

const Loading = () => {
    return (
        <div className="loading">
            <div className="loading__spinner">Cargando...</div>
        </div>
    );
};

export default Loading;
